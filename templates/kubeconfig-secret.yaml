{{- if false }}
{{/* 
This template is disabled. Create the kubeconfig secret manually using:
kubectl create secret generic mcp-bridge-kubeconfig --from-file=config=/path/to/your/kubeconfig
*/}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mcp-bridge.fullname" . }}-kubeconfig
  labels:
    {{- include "mcp-bridge.labels" . | nindent 4 }}
type: Opaque
data:
  config: {{ .Values.kubeconfig | default "" | b64enc | quote }}
{{- end }} 